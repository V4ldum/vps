apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: manganotif
  namespace: flux-system
spec:
    interval: 5m0s
    path: .
    prune: true
    targetNamespace: default
    sourceRef:
        kind: GitRepository
        name: manganotif

---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: manganotif
  namespace: flux-system
spec:
    interval: 5m0s
    url: https://github.com/V4ldum/manganotif-back
    ref:
        branch: main
    secretRef:
        name: flux-system
    ignore: |
        /*
        !/kubernetes.yml
