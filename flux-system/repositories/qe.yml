apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: qe
  namespace: flux-system
spec:
    interval: 5m0s
    path: .
    prune: true
    targetNamespace: default
    sourceRef:
        kind: GitRepository
        name: qe

---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: qe
  namespace: flux-system
spec:
    interval: 5m0s
    secretRef:
        name: flux-system
    ref:
        branch: main
    url: https://github.com/V4ldum/qe-bleeding-edge
    ignore: |
        /*
        !/kubernetes.yml
