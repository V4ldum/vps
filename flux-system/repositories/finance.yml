apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: finance
  namespace: flux-system
spec:
    interval: 5m0s
    path: .
    prune: true
    targetNamespace: default
    sourceRef:
        kind: GitRepository
        name: finance

---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: finance
  namespace: flux-system
spec:
    interval: 5m0s
    secretRef:
        name: flux-system
    ref:
        branch: main
    url: https://github.com/V4ldum/finance-back
    ignore: |
        /*
        !/kubernetes.yml
